/**app.wxss**/

Page {
  /* 所有间隙变量，从小到大 */
  --min-gap: 15.612rpx;
  --mid-gap: 31.25rpx;
  --pic-gap: 46.865rpx;
  --max-gap: 62.5rpx;

  /* 应用图标设置，最小的应用图标宽度 */
  --min-applications-item-width: 40px;
}
